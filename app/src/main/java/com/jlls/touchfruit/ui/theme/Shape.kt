package com.jlls.touchfruit.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.unit.dp

// ===============================
// GEOMETRY TOKENS
// ===============================

// Border Radius
val RadiusScreen = 40.dp      // Pantalla principal
val RadiusCard = 24.dp        // Tarjetas de acción
val RadiusPill = 9999.dp      // Botones flotantes, pills
val RadiusSm = 12.dp          // Elementos pequeños

// Shapes
val ShapeScreen = RoundedCornerShape(RadiusScreen)
val ShapeCard = RoundedCornerShape(RadiusCard)
val ShapePill = RoundedCornerShape(RadiusPill)
val ShapeSm = RoundedCornerShape(RadiusSm)